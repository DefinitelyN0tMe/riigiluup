# Private beta — keep the whole site out of every index. The nginx auth gate already returns
# 401 to crawlers; this is belt-and-suspenders. Restore Allow: / (+ sitemap) when going public.
User-agent: *
Disallow: /
